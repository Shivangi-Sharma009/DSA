#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int arr[100];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int val,num,pos;
    scanf("%d",&val);
    for(int i=0;i<val;i++){
        scanf("%d %d",&num,&pos);
        for(int j=n-1;j>=pos;j--){
            arr[j+1]=arr[j];
        }
        arr[pos]=num;
        n++;
    }
    for(int i=0;i<n;i++){
        printf("%d ",arr[i]);
    }
    
    return 0;
}
